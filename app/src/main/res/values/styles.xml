<resources>

        <!-- Base application theme. -->
        <style name="AppTheme" parent="AppTheme.Base"/>

        <!-- disable the decor-provided Action Bar -->
        <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">

                <!-- Customize your theme here. -->

                <!-- colorPrimary is used for the default action bar background -->
                <item name="colorPrimary">@color/colorPrimary</item>

                <!-- colorPrimaryDark is used for the status bar -->
                <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

                <!-- colorAccent is used as the default value for colorControlActivated which is used to tint widgets -->
                <item name="colorAccent">@color/colorAccent</item>

        </style>

        <style name="HeaderFont" parent="@android:style/TextAppearance.Small">
                <item name="android:textColor">@color/colorAccent</item>
                <item name="android:fontFamily">sans-serif-black</item>
                <item name="android:textAppearance">?android:textAppearanceSmall</item>
                <item name="android:textStyle">normal</item>
                <item name="android:paddingTop">@dimen/header_padding_top</item>
                <item name="android:paddingBottom">@dimen/header_padding_bottom</item>
        </style>

        <!-- Touch feedback in material design provides an instantaneous visual confirmation at the point of contact when users interact with UI elements -->
        <!--  Apply this functionality in your view XML by specifying the view background as: ?android:attr/selectableItemBackground for a bounded ripple.-->
        <!-- Alternatively, you can define a RippleDrawable as an XML resource using the ripple element.-->

        <!-- The following us used to style togglebutton -->
        <style name="myToggle">
                <item name="android:text">""</item>
                <item name="android:textOn">""</item>
                <item name="android:textOff">""</item>
                <item name="android:background">?android:attr/selectableItemBackground</item>
                <item name="android:layout_gravity">center</item>
                <item name="android:gravity">center</item>
                <item name="android:button">@drawable/toggle_item_details</item>
        </style>



</resources>
