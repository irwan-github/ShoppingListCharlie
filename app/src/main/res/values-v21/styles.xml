<?xml version="1.0" encoding="utf-8"?>
<resources>

        <style name="AppTheme" parent="AppTheme.Base">
                <!-- enable window content transitions -->
                <item name="android:windowActivityTransitions">true</item>
        </style>

        <style name="AppTheme.Home">
                <!-- enable window content transitions -->
                <item name="android:windowActivityTransitions">true</item>
                <item name="android:windowExitTransition">@transition/activity_fade</item>
                <item name="android:windowReenterTransition">@transition/activity_explode</item>
        </style>

        <style name="AppTheme.Details">
                <!-- enable window content transitions -->
                <item name="android:windowActivityTransitions">true</item>
                <item name="android:windowEnterTransition">@transition/activity_explode</item>
                <item name="android:windowReturnTransition">@transition/activity_fade</item>

        </style>
</resources>